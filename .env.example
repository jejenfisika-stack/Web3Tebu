# Pinata IPFS — token rahasia untuk upload file ke IPFS (server-side saja).
# Dapatkan di pinata.cloud → API Keys → JWT.
# Lokal: salin file ini jadi .env.local lalu isi nilainya.
# Produksi: set di Vercel → Project Settings → Environment Variables.
PINATA_JWT=eyJxxxxx...
